name: Build
run-name: run ${{ github.run_id }} ref ${{ github.ref_name }}

on:
  push:
    branches:
      - main
      - develop
      - feature/github-workflow
      - 'feature/**'
      - 'release/**'
  pull_request:
    branches:
      - main

env:
  DD_GIT_REPOSITORY: 'https://github.com/iterorganization/imas-data-dictionary.git'
  DD_VERSION: 'main'

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        build-type: [RelWithDebInfo]
        docs-only: [OFF]
        backend-mdsplus: [ON]
        backend-hdf5: [ON]
        backend-uda: [ON]
        build-mdsplus-models: [ON]
        python-bindings: ['no-build-isolation']
        download-dependencies: [ON]
        exclude:
          - os: macos-latest
            backend-uda: OFF

    runs-on: ${{ matrix.os }}

    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # unshallow for setuptools-scm.get_version to discover tags

      - uses: actions/checkout@v4
        if: ${{ startsWith(matrix.os, 'ubuntu-') && matrix.backend-uda }}
        with:
          fetch-depth: 0
          repository: ukaea/uda
          ref: 2.7.6
          path: third-party/uda

      - uses: actions/checkout@v4
        if: ${{ startsWith(matrix.os, 'ubuntu-') && matrix.backend-mdsplus }}
        with:
          fetch-depth: 0
          repository: mdsplus/mdsplus
          ref: stable_release-7-132-0
          path: third-party/mdsplus

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: 'pyproject.toml' # Read python version from a file pyproject.toml

      - uses: actions/github-script@v7
        with:
          script: |
            core.exportVariable('ACTIONS_CACHE_URL', process.env.ACTIONS_CACHE_URL || '');
            core.exportVariable('ACTIONS_RUNTIME_TOKEN', process.env.ACTIONS_RUNTIME_TOKEN || '');

      - name: Install linux Dependencies
        if: startsWith(matrix.os, 'ubuntu-')
        run: >
          sudo apt update && sudo apt install -y
          build-essential
          cmake
          cython3
          git
          libboost-dev
          libboost-filesystem-dev
          libboost-program-options-dev
          libhdf5-serial-dev
          ninja-build
          pkg-config

      - name: Install linux Dependencies (UDA)
        if: ${{ startsWith(matrix.os, 'ubuntu-') && matrix.backend-uda }}
        run: >
          sudo apt update && sudo apt install -y
          capnproto
          libcapnp-dev
          libspdlog-dev
          libssl-dev
          libxml2-dev
          libtirpc-dev

      - name: Install UDA Dependency
        # TODO: This should definitely be cached, or dockerized
        # TODO: can we invoke easybuild to do this pls?
        if: ${{ startsWith(matrix.os, 'ubuntu-') && matrix.backend-uda }}
        working-directory: third-party/uda
        run: >
          cmake -G Ninja -B build .
          -Wno-deprecated
          -DBUILD_SHARED_LIBS=ON
          -DSSLAUTHENTICATION=ON
          -DCLIENT_ONLY=ON
          -DENABLE_CAPNP=ON
          # -DCMAKE_INSTALL_PREFIX=install

          cmake --build build

          sudo cmake --install build

      - name: Install MDSplus Dependency
        # TODO: This should definitely be cached, or dockerized
        # TODO: can we invoke easybuild to do this pls?
        if: ${{ startsWith(matrix.os, 'ubuntu-') && matrix.backend-mdsplus }}
        working-directory: third-party/mdsplus
        run: |
          mkdir build
          cd build
          configure --disable-doxygen-doc --disable-java
          make
          sudo make install

      - name: Install macos dependencies
        if: matrix.os == 'macos-latest'
        run: >
          brew update-reset && brew install
          git
          boost
          cmake
          pkg-config
          ninja
          hdf5
          # capnp
          # libxml2
          # openssl
          # spdlog

      - name: Install windows dependencies
        if: matrix.os == 'windows-latest'
        run: vcpkg install --triplet x64-mingw-static --binarysource="clear;x-gha,readwrite"

      - name: pip install
        run: >
          pip install --ignore-installed
          "cython-cmake>=0.2.0"
          "cython>=3.0.3"
          "numpy<2"
          "scikit-build-core>=0.9.3"
          "setuptools>=64"
          "setuptools_scm>=8"

          pip list -v


      - name: Configure CMake (linux, macos)
        if: matrix.os != 'windows-latest'
        run: >
          cmake -GNinja -Bbuild
          -Wno-deprecated
          -DBUILD_SHARED_LIBS=ON
          -D "CMAKE_INSTALL_PREFIX=$(pwd)/test-install"
          -DCMAKE_BUILD_TYPE=${{ matrix.build-type }}
          -DDOCS_ONLY=${{ matrix.docs-only }}
          -DAL_BACKEND_HDF5=${{ matrix.backend-hdf5 }}
          -DAL_BACKEND_MDSPLUS=${{ matrix.backend-mdsplus }}
          -DAL_BACKEND_UDA=${{ matrix.backend-uda }}
          -DAL_BUILD_MDSPLUS_MODELS=${{ matrix.build-mdsplus-models }}
          -DAL_PYTHON_BINDINGS=${{ matrix.python-bindings }}
          -DDOWNLOAD_DEPENDENCIES=${{ matrix.download-dependencies }}
          -DDD_GIT_REPOSITORY=${{ env.DD_GIT_REPOSITORY }}
          -DDD_VERSION=${{ env.DD_VERSION }}
          -DBoost_NO_BOOST_CMAKE=ON

      - name: Configure CMake (windows)
        if: matrix.os == 'windows-latest'
        run: >
          XDR_ROOT=extlib/install VCPKG_ROOT=C:/vcpkg cmake -G "MinGW Makefiles" -B build
          -DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake
          -DBUILD_SHARED_LIBS=ON
          -D "CMAKE_INSTALL_PREFIX=${{github.workspace}}/test-install"
          -DCMAKE_BUILD_TYPE=${{ matrix.build-type }}
          -DDOCS_ONLY=${{ matrix.docs-only }}
          -DAL_BACKEND_HDF5=${{ matrix.backend-hdf5 }}
          -DAL_BACKEND_MDSPLUS=${{ matrix.backend-mdsplus }}
          -DAL_BACKEND_UDA=${{ matrix.backend-uda }}
          -DAL_BUILD_MDSPLUS_MODELS=${{ matrix.build-mdsplus-models }}
          -DAL_PYTHON_BINDINGS=${{ matrix.python-bindings }}
          -DDOWNLOAD_DEPENDENCIES=${{ matrix.download-dependencies }}
          -DDD_GIT_REPOSITORY=${{ env.DD_GIT_REPOSITORY }}
          -DDD_VERSION=${{ env.DD_VERSION }}
          -DBoost_NO_BOOST_CMAKE=ON

      - name: Build
        run: cmake --build build

      - name: Install test-install
        run: cmake --build build --target install

      - name: Run pytest, generate clover
        if: matrix.os == 'ubuntu-latest'
        run: |
          python -m venv --system-site-packages $(pwd)/test-install
          source $(pwd)/test-install/bin/activate
          pip install --ignore-installed "pytest-cov" "pytest>=8" "coverage2clover"
          pip list -v
          python -c "import imas_core ; print(imas_core)"
          pytest --junitxml results.xml --cov imas_core --cov-report xml --cov-report html
          coverage2clover -i coverage.xml -o clover.xml

#      - name: Test
#        working-directory: build
#        # Execute tests defined by the CMake configuration.
#        # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
#        run: ctest
